import { makeStyles } from "@mui/styles"

export const SignUpStyles = makeStyles((theme) => ({
    rootStack: {
        alignItems: "center",
        justifyContent: "center",
        padding: "5px"
    }
}))